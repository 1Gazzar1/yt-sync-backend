# YT Music Sync

## TO-DO

1. find a way to extract mp3 metadata and fix the /:jobId/:songId endpoint
2. find a way to get the video thumbnail efficiently to fix /sync/metadata
3. test the cleanup.js job
