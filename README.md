# YT Music Sync

## TO-DO

1. make sure you implement the archiver package in the /batch endpoint
2. find a way to extract mp3 metadata and fix the /:jobId/:songId endpoint
3. find a way to get the video thumbnail efficiently to fix /sync/metadata
4. test the cleanup.js job
